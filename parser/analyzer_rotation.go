package main

import (
	"fmt"
	"os"
)

func AnalyzeRotation(

	ctx *AnalyzerContext,

	result *Result,

) {

	fmt.Fprintln(os.Stderr)

	fmt.Fprintln(os.Stderr, "========== Rotation Analyzer ==========")
}
